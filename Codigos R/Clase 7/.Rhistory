ozono_mes <- ozono_new %>%
group_by(mes) %>%
summarise(
o3_medio_mes = mean(o3),
n_registros_mes = n())
ozono_mes %>%
ggplot(aes(x = mes, y = o3_medio_mes)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 3) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por mes, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora <- ozono_sinna %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>%
group_by(estacion, fecha_hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
source(here::here("clases/clase7/estadistica_basica.R"))
source(here::here("Codigos R/Clase 7/estadistica_basica.R"))
pA
pB
pC
pD
pE
ozono_dia <- ozono_sinna %>%
mutate(dia = as_date(fecha)) %>%
group_by(estacion, dia) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 1440
.groups = "drop"
)
ozono_dia %>%
ggplot(aes(x = dia, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_dia %>%
pivot_longer(c(media, p95, maximo), names_to = "estadistico", values_to = "o3") %>%
ggplot(aes(x = dia, y = o3, color = estadistico)) +
geom_line() +
facet_wrap(~ estacion, ncol = 1, scales = "free_y") +
theme_minimal() +
labs(title = "Agregación diaria: media, p95 y máximo por estación",
x = NULL, y = "O3 (µg/m³)")
grilla_horaria <- seq(min(ozono_hora$fecha_hora), max(ozono_hora$fecha_hora), by = "hour")
ozono_8h <- ozono_hora %>%
select(estacion, fecha_hora, media) %>%
complete(estacion, fecha_hora = grilla_horaria) %>%   # inserta NA en horas ausentes
arrange(estacion, fecha_hora) %>%
group_by(estacion) %>%
mutate(
# stats::filter, no dplyr::filter (mismo nombre, función distinta)
media_8h = as.numeric(stats::filter(media, rep(1 / 8, 8), sides = 1))
) %>%
ungroup()
max_8h_dia <- ozono_8h %>%
mutate(dia = as_date(fecha_hora)) %>%
group_by(estacion, dia) %>%
summarise(
max_8h  = if (all(is.na(media_8h))) NA_real_ else max(media_8h, na.rm = TRUE),
horas_validas = sum(!is.na(media_8h)),
.groups = "drop"
)
ggplot(max_8h_dia, aes(x = dia, y = max_8h, color = estacion)) +
geom_line() +
geom_hline(yintercept = 100, linetype = "dashed") +   # guía OMS 2021: 100 µg/m³
theme_minimal() +
labs(title = "Máximo diario de la media móvil de 8 h",
x = NULL, y = "O3 (µg/m³)")
source(here::here("Codigos R/Clase 7/mapa.R"))
source(here::here("Codigos R/Clase 7/mapa.R"))
source(here::here("Codigos R/Clase 7/mapa.R"))
source(here::here("Codigos R/Clase 7/mapa.R"))
#| label: fig-mapa
#| fig-cap: "Ozono en Montevideo, ene–abr 2024"
#| fig-width: 8
#| fig-height: 6
#| screenshot.opts: !expr list(delay = 3, vwidth = 1000, vheight = 750)
mapa
ozono_limpio %>%
group_by(estacion) %>%
summarise(
n_filas  = n(),
n_na     = sum(is.na(o3)),
prop_na  = mean(is.na(o3)),
.groups = "drop"
)
## 9.2 ¿Cuándo faltan? Proporción de NA por día
na_dia <- ozono_limpio %>%
mutate(dia = as_date(fecha)) %>%
group_by(estacion, dia) %>%
summarise(prop_na = mean(is.na(o3)), .groups = "drop")
ggplot(na_dia, aes(x = dia, y = prop_na)) +
geom_col(fill = "grey40") +
facet_wrap(~ estacion, ncol = 1) +
theme_minimal() +
labs(title = "Proporción de minutos sin dato, por día y estación",
x = NULL, y = "Proporción de NA")
## 9.3 Rachas de NA consecutivos (rle = run length encoding)
rachas <- ozono_limpio %>%
arrange(estacion, fecha) %>%
group_by(estacion) %>%
reframe({
r <- rle(is.na(o3))
tibble(faltante = r$values, largo = r$lengths)
})
rachas %>%
filter(faltante) %>%
mutate(tipo = cut(largo, breaks = c(0, 1, 10, 60, 1440, Inf),
labels = c("1 min", "2-10 min", "11 min-1 h", "1 h-1 día", "> 1 día"))) %>%
count(estacion, tipo, name = "n_rachas")
## 9.4 Faltantes implícitos: minutos que no están en el archivo
ozono_limpio %>%
group_by(estacion) %>%
summarise(
primero   = min(fecha),
ultimo    = max(fecha),
esperados = as.numeric(difftime(max(fecha), min(fecha), units = "mins")) + 1,
presentes = n_distinct(fecha),
.groups = "drop"
) %>%
mutate(implicitos = esperados - presentes,
prop_implicitos = implicitos / esperados)
INICIO <- as.POSIXct("2024-01-01 00:00:00", tz = tz(ozono_limpio$fecha))
FIN    <- as.POSIXct("2024-04-30 23:59:00", tz = tz(ozono_limpio$fecha))
ESPERADOS <- as.numeric(difftime(FIN, INICIO, units = "mins")) + 1
cobertura <- ozono_limpio %>%
group_by(estacion) %>%
summarise(presentes = n_distinct(fecha),
validos   = sum(!is.na(o3)),
.groups = "drop") %>%
mutate(esperados         = ESPERADOS,
sin_fila          = esperados - presentes,
fila_sin_medicion = presentes - validos,
cobertura         = 100*round(validos / esperados, 3))
cobertura
ozono_hora <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora %>%
ggplot(aes(x = hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora <- ozono_sinna %>%
mutate(fecha_hora = floor_date(fecha_hora, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora <- ozono_sinna %>%
mutate(fecha_hora = floor_date(fecha_hora, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora <- ozono_sinna %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(hora, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(hora, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha_hora, unit = "hour")) %>% # Operacion 1: crea variable
group_by(estacion, fecha_hora) %>% # Genera valores agrupados
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora <- ozono_sinna %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, fecha_hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, fecha_hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 %>%
ggplot(aes(x = hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(hora = floor_date(hora, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 <- ozono_new %>%
mutate(hora = floor_date(fecha_hora, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 <- ozono_new %>%
mutate(hora = floor_date(fecha_hora, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora <- ozono_sinna %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, fecha_hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 <- ozono_new %>%
mutate(hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 %>%
ggplot(aes(x = hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, fecha_hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, fecha_hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 %>%
ggplot(aes(x = hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 <- ozono_new %>%
mutate(fecha_hora = floor_date(fecha, unit = "hour")) %>% #Operacion 1: crea var
group_by(estacion, hora) %>%
summarise(
media  = mean(o3),
maximo = max(o3),
p95    = quantile(o3, 0.95),
n_min  = n(),               # de un máximo de 60
.groups = "drop"
)
ozono_hora2 %>%
ggplot(aes(x = fecha_hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
ozono_hora2 %>%
ggplot(aes(x = hora, y = media)) +
geom_line(color = "blue", linewidth = 1) +  # Une los puntos con una línea
geom_point(color = "red", size = 1) +       # Dibuja los puntos individuales
theme_minimal() +                           # Aplica un diseño limpio
labs(
title = "Concentración de Ozono en el ambiente, por hora, 2024",
x = "Fecha",
y = "Valores"
)
